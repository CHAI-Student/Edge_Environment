# AI Smart Vending Machine - Core Dependencies
# For Jetson Orin Nano: Use system packages for torch, numpy, opencv
# Create venv with: python3 -m venv --system-site-packages .venv

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP Clients
httpx>=0.24.0
aiohttp>=3.8.0
httpx-sse>=0.3.0

# Image Processing (use system opencv on Jetson)
pillow>=10.0.0

# Serial Communication
pyserial>=3.5
pyserial-asyncio>=0.6

# Data Parsing
construct>=2.10.70
pyyaml>=6.0.0

# File Upload
python-multipart>=0.0.6

# AI/ML (ultralytics only - torch comes from system on Jetson)
ultralytics>=8.0.0

# MQTT
aiomqtt>=2.0.0
